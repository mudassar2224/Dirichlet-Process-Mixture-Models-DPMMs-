# Dirichlet-Process-Mixture-Models-DPMMs-
This project is an NLP-based semantic movie search engine that recommends movies based on meaning rather than exact keyword matching.  It uses sentence embeddings + unsupervised clustering (DPMM) to group and retrieve similar movies.
